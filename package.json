{
  "name": "email-sending-service",
  "version": "1.0.0",
  "description": "A resilient email sending service with mock providers, retry logic, fallback, idempotency, rate limiting, status tracking, circuit breaker, logging, and queue.",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "npx tsc",
    "start": "npx tsc && node dist/src/index.js",
    "test": "jest"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^22.15.30",
    "jest": "^29.5.0",
    "ts-jest": "^29.3.4",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@vercel/node": "^1.11.0"
  }
}
